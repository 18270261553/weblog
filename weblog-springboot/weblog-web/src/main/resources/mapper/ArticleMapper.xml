<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.quanxiaoha.weblog.common.domain.mapper.ArticleMapper">

    <!-- 按关键词搜索文章 -->
    <select id="searchByKeywords" resultType="com.quanxiaoha.weblog.common.domain.dos.ArticleDO">
        SELECT id, title, description, create_time, update_time, read_num, is_deleted
        FROM t_article
        WHERE is_deleted = 0
        AND (
        <foreach collection="keywords" item="keyword" separator=" OR ">
            title LIKE CONCAT('%', #{keyword}, '%')
            OR description LIKE CONCAT('%', #{keyword}, '%')
        </foreach>
        )
        ORDER BY create_time DESC
        LIMIT 5
    </select>

</mapper>